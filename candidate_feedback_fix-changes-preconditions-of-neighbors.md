---
name: fix-changes-preconditions-of-neighbors
description: 修好一个东西会改变周边代码的前提——尤其"修好一条从来没生效过的通道"和"改变某物的归类"，要专门去找依赖它旧状态的代码
metadata:
  node_type: memory
  type: feedback
  status: candidate
  signal: 3-rework-nearmiss
  originSessionId: 919bccc7-72d9-47ac-8059-5044e28c41c5
  modified: 2026-08-13T01:25:32.706Z
---

**改动的爆炸半径不只是"我改的这段代码对不对"，还包括"谁在依赖它原来的样子"。**
两个高发形态，2026-08-13 同一批改动里各中一次，都是独立评审官抓的、我自查全没看见：

**形态一：修好一条从来没生效过的通道 → 依赖它"失效"的代码会翻车。**
G3 的 session-spool 通道因类型不匹配自提交起从未投递成功。我修好它之后，
`claimCodexSession`（启发式兜底）**永远命不中自己那行**了——因为 spool 会先把精确 sid
加进 `claimedSids`，而它的 tick 只有"页签关闭 / 命中一行"两个出口。后果：永久轮询；
更糟的分支是命中用户在别处跑的同 cwd 会话，**把精确 sid 覆盖成错的**。
我还在注释里写"spool 一到就被精确值覆盖"——**时序正好写反了**（spool 先到）。

**形态二：把某物换个归类 → 一个一直存在但对它无害的洞，突然对它生效。**
我把 x.com/youtube.com 从公共分区挪进专属分区（修的是面板导航被守卫误劫）。
而跨分区守卫只挂了 `will-navigate`、对 **302 服务端重定向完全失明**——这个洞对
figma/claude.ai 一直存在。x.com 原本就归公共分区、**无物可泄**；挪进专属分区后，
登录态第一次有了"泄到公共分区"的可能，而 X 站内每条外链都走 `t.co`（判公共分区），
正是最容易踩的路径。**改动本身是对的，它只是把一个既有缺陷的影响面接通了。**

**How to apply（改完先问这三句，再谈测试）：**
1. **"这东西原来是坏的/不生效的吗？"** 是 → 列出所有依赖它坏掉的代码路径（兜底、重试、
   超时、fallback），逐个看它们的退出条件还成不成立。
2. **"我改的是归类/分区/优先级吗？"** 是 → 把被重新归类的对象代进**所有**按该归类分支的
   既有逻辑，特别是那些"因为它原来在 A 类所以不用管"的守卫。
3. **"有没有哪个既有缺陷，因为我这次改动而第一次变得可达？"** 这条自查最容易漏——
   缺陷不是我引入的，但可达性是。
4. 注释里写"X 会覆盖 Y"这类**时序断言**前，把两条路径的实际触发顺序跑一遍或读一遍；
   我这次凭直觉写反了，而它正好是判断谁赢的关键。

相关：[[feedback_verify-your-verification]]（同批还抓出 4/5 自设变异不转红）、
[[feedback_redteam-own-analysis-before-shipping]]（我的自查 34/34 全绿，没覆盖 302 这条路）。
