---
name: candidate_feedback_coherent-at-my-layer-is-not-execution-truth
description: "一份东西在我这一层自洽 ≠ 它真会那样发生——2026-08-14 记忆治理计划四轮红队，同一根因换了四张脸（观测当因果/散文当执行面/验收无区分度/跨条目无账），每次的解药都是\"去看那个动作真正发生的地方\""
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 8b0831e2-defb-4aa5-b610-3c1d1373406f
  modified: 2026-08-15T03:11:01.859Z
---

2026-08-14/15，给 luca_gstack 记忆系统做体检 + 治理计划。四轮独立红队，每轮都出真 BLOCKER
（3/3/4/5），我逐条独立复核后**全部成立**。测量层四轮零推翻——数字是硬的。**错的一直是同一件事，
只是每轮换一张脸。**

## 四张脸

| 轮次 | 我写下的 | 真相 | 我漏做的动作 |
|---|---|---|---|
| v1 | `promotion_ready==0` → "代码静默死锁" | 是**设计态**，人工闸门 `--set-stable` 存在可用，那个 bug 24 天前已修 | **读代码** |
| v2 | 把合并落点制度写进 `extraction-bar.md`，称"最高杠杆零成本" | 该文件**不在任何注入路径上**，Stop hook 只注入 ≤900 字符短指针，里面没有一句关于合并 | **查注入路径** |
| v3 | 验收「semantic stable fact 不得被挤出 top5」 | 在含"什么都不做"的**四档配置里全部通过**，零区分度（v4 改成 ≤2/40，又变成必然失败——同病反面） | **在四档上真跑一次** |
| v4 | G2 收窄 + G5 缩范围，各自都合理 | **相乘后净收益 −0.52%，换个选法 +0.55%**（注入面反而变大）；另：G1 沙箱验得扎实但沙箱里没有 `daily_governance` 这个每日无条件跑 `--promote-ready --archive-reviewed` 的并发写者 | **跨条目算账** |

## 根因（一句）

**每一层，我都在用"我这一层内部自洽"冒充"它真的会那样发生"。**
散文通顺、逻辑闭合、数字对得上——这些都只证明我没自相矛盾，不证明世界会照这么运行。

## 解药（每次都是同一个动作）

**去看那个动作真正发生的地方。** 具体到四张脸：
- 断言某机制坏了 → **读那段代码**，而不是从状态输出反推
- 写下一条规则 → **grep 它的注入/消费路径**，确认有东西会读到它（写进不被注入的文件 = 安慰剂）
- 写下一条验收 → **在"什么都不做"的对照档上跑一遍**；四档全过和四档全不过是同一种病，
  判据必须携带"改对了没有"的信息
- 多个阶段各自 OK → **逐对过三问**：它们改同一个量吗（字符/队列/文件）？后写的那条是否废止了
  先写的那条的验收？**有没有第三方进程也在写这个量？**

## 自检句

> **「如果我这条是错的，会出现什么现象？我去看过那个现象吗？」**

看过 = 跑过命令 / 读过那几行 / 在对照组上试过。没看过就写成"因为…所以…"，四次里有四次是错的。

## 附带发现（值得单独记住）

- **验收标准本身是最容易骗自己的地方**：它由我写、由我判，且写的时候还没有被测对象。
  三版验收里，一版四档全过、一版必然失败、一版锚在我自己定义的数字上（换个等价实现就复现不出）。
  → 验收要么锚在**署名/集合相等**这类不可伪造的东西上，要么就明写"这里靠人工签字"。
- **cron / hook 是隐形的并发写者**：`daily_governance.py:817` 每天无条件 `--promote-ready
  --archive-reviewed`，由 `session-restore.mjs` 静默 detached spawn。我设计的"人类逐条裁决"
  跨日就会被它替你清空队列，而我的验收（队列 29→0）**正是它能伪造的终态**。
  凡设计跨 session 的人类动作，先问"这段时间里有没有别的进程在写同一个文件"。
- **编排者自己的 Stop hook 属于带外写入**：给在飞 agent 的 provenance 台账只登记"我显式编辑了
  什么"是不够的，hook 的自动写入也是我这个进程的副作用。（扩展 [[feedback_orchestrator-provenance-ledger]]）

相关：[[feedback_verify-your-verification]]（验证要有区分度／变异要有牙）、
[[feedback_redteam-own-analysis-before-shipping]]（上报前先红队自己的结论）、
[[verify-runtime-not-spec]]（文档规定 ≠ 实际行为）。本条是它们的**上位形态**：
那几条讲"别信某个来源"，本条讲"别信我自己这一层的自洽"。
