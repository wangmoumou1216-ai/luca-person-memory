---
name: scope-diff-measurement-to-fresh-output
description: "在累积型载体（scrollback/日志/append-only 状态）上做差分测量前必须把量程限定到本次新产出——历史残留会让『变了』成为假信号，据此下的结论会反向"
metadata:
  node_type: memory
  type: feedback
  signal: 3
  status: candidate
  originSessionId: cdc597d5-6dea-4db3-b1da-ed98a0a0e883
  modified: 2026-08-04T02:00:19.714Z
---

2026-07-31 muse 外观↔Claude CLI 主题绑定：要判断「已跑会话里重选 /theme Auto 会不会让 CLI
重新探测底色」，我读 xterm 缓冲区里的真彩色板，重选前后一比——**色板变了** → 判定"重选有效"，
写进了用户可见的 toast 建议并提交。

后来收紧量程：只读**当前视口**（而非整个 buffer），并用 `/help` 铺满一屏保证样本量。结果
深/浅两档下 767 vs 775 个真彩单元**色板逐色相同** → 重选根本没触发重探测，**结论反向**。
原来那个"变了"来自 scrollback：旧主题渲染的历史行一直躺在 buffer 里，我读的是**历史并集**，
新颜色一出现并集就变，与"主题是否切换"无关。toast 因此改口重提（muse 3de0089 → 263969f）。

**Why:** 差分测量的隐含前提是"两次读到的都是当前状态"。载体一旦是**累积型**（终端 scrollback、
日志文件、append-only 表、带历史的缓存），读全量拿到的是**状态并集**而非当前状态——而并集
**只增不减**，于是"变了"几乎恒真、"没变"才是真信号。危险在于它给出的是**假阳性**：证实了我
本来就想要的结论（"我的建议有效"），所以不会触发怀疑，直接被当成证据发货。

**How to apply:**
① **动手测之前先问一句"这个载体会不会留历史"**。会 → 量程必须限定到本次新产出：视口而非
全 buffer、本次运行的日志切片而非整份文件、按时间戳/序号切窗而非 `SELECT *`。
② **优先用"没变"当阳性证据，而不是"变了"**。在累积载体上"变了"廉价到没有信息量；把假设
写成"若机制生效则某个**限定量程**内的值必然不同"，再去证伪。
③ **样本量要报出来**。本轮翻案靠的是"767/775 个单元逐色相同"这种带分母的陈述；上一版深色档
只采到 **1 个颜色**我却照样下了结论——单样本比较等于没比较，出结论前先看分母。
④ **同一结论翻案后，检查它是否已经流到下游**。这条错误判断已经进了 toast 文案并 commit，
翻案时必须连带改口+重提，不能只在对话里更正（自陈缺陷≠处理缺陷）。

关联 [[feedback_verify-your-verification]]（信"绿/一致"前先确认脚本真考验目标；本条补的是
"量程"这一维）、[[feedback_disclosure-is-not-remediation]]（翻案要落到已发货的产物上）、
[[candidate_feedback_control-group-before-inferring-cause]]（归纳时设对照；本条是测量时限量程，
两者都属"证据取得方式本身有偏"）。
