---
name: surface-buried-value-before-deleting
description: "整合重复资产：被覆盖的才是废资产可删，没覆盖的要逐条判价值，有价值的必须挂到会被执行的地方——目标是价值被发现，不是留或删的二选一"
metadata:
  node_type: memory
  type: feedback
  signal: 1
  originSessionId: 6835476b-22b2-4b83-a52f-72c1a0baf4b1
  modified: 2026-08-03T08:06:23.701Z
---

luca 2026-08-03 原话（我正要删三个零使用 skill 时）：「**如果全覆盖了，说明被覆盖的是废资产，
是不要的。如果没覆盖，没覆盖的地方是不是有价值的？如果有价值应该怎么处理，才能体现价值？
请你按照价值维度思考。我要的是价值被发现。**」

这是判据结构，不是一次决定：
① **被别处覆盖 → 废资产 → 删**（覆盖本身就证明它是重复建设）
② **没被覆盖 → 逐条判价值**，有价值 → **找到能让它生效的落点**；无价值 → 才删
③ 目标是**价值被发现**，不是"留还是删"的二选一——留着不被执行 = 没救回来。

**Why:** 我当时的依据是「功能已被 redteam 覆盖」——**一句自己没验证的概括**（同 session 第三次
用概括代替逐条差集）。luca 一问「redteam 把价值都覆盖了对吗」我去核，实测覆盖率只有 50-65%：
taste-review 14 条、design-review 4 个维度、handoff-review 的整套机制都没被覆盖。**按我原方案删下去，
真价值会跟着废资产一起消失，而且我会以为自己做对了。**

**How to apply（操作顺序，缺一步都会漏价值）：**
1. **先做覆盖率差集，逐条不概括**——「A 的每个可执行条款，在 B 里是否存在」，存在给行号，
   不存在明确写「无」。概括（"大部分在别处"/"唯一独有是 X"）在这一步一律不算数。
2. **对"没覆盖"的每条问：它比现状强在哪**。本轮判高价值的四条各有明确对比——
   `无来源 AI 输出 {个数，必须=0}`（现状只有定性"一票否决"，一个"是"就过）；
   `Slop ≥3 返工 / 1-2 警告`（现状只说"发现即移除"无分级，没人会为 1 个返工）；
   取证表（现状一行一问产出**印象**，取证表强制写"每步等待时间/结果可见位置"这类**中间数据**）；
   oracle-vs-taste（"什么能自动修/什么必须交人"，每个自动化环节都要重答一次的判断）。
   答不出"强在哪"的 → 那才是废资产（本轮判废：审查对象三选一、模式≠场景澄清[坐标轴本身是混淆源]、
   产出格式模板、workflow-state 豁免声明）。
3. **落点必须是「会被执行的地方」，不是「存起来的地方」**——这一条最容易做错。
   本轮红队已实证：迁进 reference 但不改真正产出内容的模板 = 生产者不会输出它模板里没有的字段，
   收益为零。正确落点是**必经环节**：量化门与 Slop 分级 → design-brief Phase 4（全框架产出最多的
   skill 的必经阶段）；判据挂载 → redteam（唯一活着的评审入口，用量最高）。判据：**这个位置会不会
   在没人特意想起它的时候被执行**。
4. **先迁后删，顺序不可逆**——先删就找不回来了。迁完逐条验新宿主里真的存在，再动删除。

**副产品**：走完这四步，"该不该删"往往自动有答案——价值都挂出去之后，剩下的壳自己就变成了
纯流程外壳，删它不再需要犹豫。

---

## 判据必须是「场景」不是「多久没用」（luca 2026-08-03 当场纠正）

原话：「**我有些没用到，是因为我还没走工作场景。都是在修框架。所以用的少。但是如果因为这样就
砍掉了高价值的能力，你是不称职的。所以不要只看数字。要按照场景和价值去衡量。**」

**正确判据**：不是「N 天没被用」，是「**该用它的场景发生过吗**」——
- 场景发生过 N 次、它一次没被调用 → 才是它的问题（可判死）
- 场景一次都没发生 → 零使用什么都不能证明，**不得据此处置**

本轮实测（该做而当时没做的那一步）：设计链 7 月仍在跑（design-brief 5 份 / tech-spec 2 份），
同期 design-review / taste-review / handoff-review 为 0 → 场景发生 5 次而零调用，判死成立。
但同一 69 天里，一手用户研究、内容规范设计这些场景**一次都没发生**（episodic 最近 12 条
100% 是框架/工程），所以 research-kit / insight-synthesis / ux-writing 的零使用**毫无信息量**。
我当时没误伤它们，靠的是"年龄太新（13-14 天）"这个**粗糙代理**——纯运气；它们再老 60 天，
同一把错尺子就会把它们判死。

**操作**：任何「零使用 → 处置」的判断，先跑一次场景计数——统计该能力的**触发场景**在观察窗口内
发生了几次（用上下游产物做代理：如设计链看 design-brief 产出数、研究链看 research/ 产出数），
分母为 0 就停手，别用"多久没用"当分子。luca 明确把这条定性为**称职与否**的分界。

关联 [[feedback_redteam-own-analysis-before-shipping]]（否定性断言与"用概括代替差集"的同族根因）、
[[candidate_feedback_control-group-before-inferring-cause]]（归纳死因要设对照组）、
[[feedback-adoption-ambition]]（母原则：手段性约束不得否决价值）。
