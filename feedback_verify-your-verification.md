---
name: verify-your-verification
description: "信\"绿/一致\"之前先确认验证本身真在考验目标——fixture 真存在、有区分性用例;别被空跑的测试脚本自骗;GUI产出'编译过+不崩'≠验证,要截运行态+驱动交互"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 79705b29-d520-4e9d-917b-e04b554becdf
  modified: 2026-08-15T12:52:08.580Z
---

报告任何"已验证/全过"之前，**先确认验证脚本本身真的在考验目标**，否则一个空跑的测试比没测更坏——它制造假信心。

**Why:** 2026-06-01-02 修 CLI 提示器，我**两次**写出空跑的 A/B 验证脚本：① 把函数定义当命令传(`command not found`)；② scenario 显示名带后缀、查的目录和造 flag 的目录不一致。两次 flag 都没造进去，每个场景都跑空目录→全返回相同的 idle/"一致"，看着像"全过"，差点当成结论上报。用户问"你确定？/验证了吗？"探的正是这个。

**延伸（2026-06-16，重构类自查不够）：** 对一次多文件重构(open-design 改桌面端优先)我做了 grep+read「一致性」自查、两次都判「干净」——但**一致性绿 ≠ 流程能跑**。上独立对抗 subagent(执行轨迹/一致性/事实核查三镜头)一遍就抓出我漏的：BLOCKER(deferred 流程的 slug 没定义/没持久化→日后 recover 找不到项目)、recover 无产物守卫(会把 404 落盘再标 DONE)、一行我自以为已清掉的旧根因仍在。**grep/读 只验表面一致，验不出语义/执行缺陷**；非平凡重构/流程改动要 (a)端到端模拟跑一遍(把自己当执行者从 0 走到 recover) + (b)上独立对抗 review，别拿一致性自查当「验过了」。关联 [[redteam-own-analysis-before-shipping]]、reproduce-real-env-not-approximation。同日又复发一次(研究情报官)：对刚建的「自动 absorb 候选」我只**孤立**测了 parseAbsorbBlock 和「模型会吐块」就反复声称已验证；上对抗审计一跑，发现 grounding 自愈重写 Final 时会把 absorb 块丢掉——bug 恰好藏在 self-heal×absorb 两个功能的**接缝**，那条集成路径我从没测过。新切面：**组件单测全绿 ≠ 集成路径对；凡新功能与既有机制(尤其会重写/拦截输出的那种)交互，必测合并路径，别拿各自孤立的绿当「整体验过」。**

**延伸（2026-06-25，GUI 盲发复发）：** 做 todo-capsule 原生 macOS app 时，我拿「`swift build` 通过 + 启动不崩」冒充「做好了」连发两版，被用户当场全打回：hover 移开不收起、勾选删不掉、阴影是矩形带四角、placeholder 黑字——**全是编译/启动验不到的运行态缺陷**。教训：**GUI/视觉产出，「编译过+不崩」≠ 验证**；交付前必须真看它渲染、真驱动交互。本次奏效的自验手段：`screencapture -R <区域>` 截运行态逐版核对视觉(注意取真实 `NSScreen.main.visibleFrame`，多屏/镜像时 Finder 报的尺寸会骗你)；加 `TC_DEBUG_MODE` 环境变量直接注入某状态(peek/capture/delete/multiadd)+ 一个 debugFreeze 锁住状态好截图；`CGWarpMouseCursorPosition` 移光标触发 hover，配合 tick 里 NSLog 出 `NSEvent.mouseLocation`/mode 验证状态机真转换(CGWarp 截图本身会漂、不稳，要以 log 为准)；改完 build→截图/log→对照参照→再改。别拿编译当做好，别拿孤立逻辑当交互验过。关联 [[1to1-ui-replication-method]]（同期复发：交付近似而非 1:1）。**（2026-06-26 续，终结多屏盲改的可靠取真值法）** 上面 `-R visibleFrame` 多屏/外接屏会落错屏，我据此连改 4-5 轮 idle 药丸阴影全在盲改、被用户反复打回「还有棱角」。真正可靠：swift `CGWindowListCopyWindowInfo(.optionOnScreenOnly,…)` 按 `kCGWindowOwnerName` 拿目标窗口的 `kCGWindowNumber`+`kCGWindowBounds` → ① `screencapture -x -o -l<windowID>` 抓窗口**本体缓冲**像素（含 SwiftUI 自绘阴影，与窗口在哪块屏无关，看阴影形状最可靠）② `screencapture -R<该窗口 bounds±margin>` 抓**合成到真实桌面**的样子（看它跟屏缘/壁纸的关系）。靠这个一眼看穿真 bug：之前「把窗口推出屏外躲开阴影裁切」是错的——**屏幕物理边缘会硬裁任何越界阴影**，把裁切线正好摆在屏缘（可见竖直棱角）；正解相反，让贴边元素离屏缘留出 ≥ 阴影弥散范围(~1.5×blur radius)，让阴影完整待在屏内。教训沉淀：**多屏下视觉验证别用屏幕坐标区域截图，用窗口 ID 截窗口本体**，这是几轮盲改的真正解药。

**延伸（2026-08-07，零回归对照只测了输入面）：** 把一段既有逻辑搬进新模块时，声称"零回归"的证据
必须覆盖被替换代码的**全部可观察行为**，而不只是最显眼的那个。本次把会话列表逻辑搬进 provider，
我写的零回归断言只比对了 **argv**（输入面），漏了**返回数据的排序与截断**（输出面）——实现里丢了
原有的 `.sort(b.mtime-a.mtime).slice(0,40)`、返回 100 条未排序，而测试**全绿**。抓出它靠的是顺手
打印真实条数（`claude 会话数: 100`）觉得不对劲，不是靠断言。做法：迁移前先逐行读原实现、列出它的
**可观察行为清单**（返回什么形状/排序/上限/边界/副作用），逐条写断言；另外 `every()` 这类**对空数组
恒真**的判据必须配一条 `length>0`（同轮踩到：codex 会话一条都没列出来时 E2 照样 PASS）。

**How to apply:** 信任任何测试/验证结果前：(1) 先断言**输入确实存在**（如 `flag 数应=N`、`ls` 目录非空）；(2) 至少放一个**区分性用例**，它的期望结果要和别的不同——如果所有用例都返回同一个值(全 idle / 全 pass)，立刻怀疑脚本根本没在考验任何东西；(3) 优先用项目自带测试套件 + 真实运行时，少用临时拼的 harness；(4) **最强证法是变异测试**——当你声称"这测试验证了这个修复"，就**逐个把修复改回 bug，确认测试如期 FAIL，再恢复**；改不坏代码却让测试 FAIL，说明测试根本没在测它。2026-06-07 加固 luca_gstack 自成长系统时，5 个修复逐个变异(改回 bug→测试 FAIL→恢复→9/9 PASS)，证明整套回归网非"窄 fixture 假绿"，直接反制本记忆的原始教训。更深一层：把用户的怀疑当默认动作——说"done"之前先问"什么情况会让这句话为假？我真查了那个情况吗？"。相关 [[run-tests-before-claiming-done]]、[[evidence_grounded_debate]]、[[no_confirmation_loops]]。**（UI 哨兵切面）** 自动化 UI 哨兵要断言**渲染现实**——`getComputedStyle().width/height`、`offsetWidth`、可见性——不只 `.classList.contains('active')`；class 被 toggle ≠ 元素真展开（CSS 让它 0 尺寸照样全绿、整组件不可用却假绿）。可见性/可发现性的入口侧见 ui-entry-discoverability。

**延伸（2026-07-03，luca 明确指示：验证者必须独立于修复者）：** luca 明确要求"修复完不能直接完事,要检查,确保真的解决问题...你的测试还不能自己测"——这次全量框架 review 修复（13项）逐项派独立、不知情的 agent（不告诉它我怎么改的，只给原始 finding + 现状让它重新判断）去核验，而不是自己跑测试自己签字。**结果证明这不是走形式**：4/6 组一次通过，但**2 组被独立核验真的抓出"部分解决"**——一处是我只改了文档中段的冲突点、末尾速查清单原样重复了同一处冲突表述没同步；另一处更关键，我只堵了 route-guard.mjs 里的一条门禁路径，独立核验读了 CLAUDE.md 另一处独立的"Plan Agent 5条件"，发现那条对目标 skill 恒真、会从另一条路径把同一个问题弹回来——这类"改了 A 处、B 处独立触发同一后果"的遗漏，靠自己回读代码基本不会主动怀疑到"是不是还有平行的第二条路径"，因为自己带着"已经解决了"的锚定去看，独立 agent 没有这个锚定包袱。**How to apply 补充：** 别人明确要求"测试不能自己测"时，字面执行——每个声称修复的点，起一个不知道你怎么改的独立 agent，只给它"原始问题是什么"+"现在的文件/代码状态"，让它自己判断是否真解决，而不是喂给它"我做了 X，对不对"这种会诱导它顺着你思路点头的问法；核验发现"部分解决"时，继续用同样的独立-agent 模式核验补丁本身，不能因为已经"修过一轮"就对第二轮补丁降低标准自己签字了事。

**延伸（2026-07-22，用容易过的那套验收冒充另一套）：** 一份 Plan Agent 计划里同时写了**两套验收**——块 3 的 bash 断言（可机判：文件存在/URL 总数/语法合法）和每个 Phase 的**阶段门控**（质量判据：「每束 ≥5 条带 URL 的 finding」）。我只跑了 bash 那套（A3=全文 ≥20 个 URL，实得 34 轻松过）就当门过了，宣布 Phase 完成；用户一句「我怎么觉得你没有按照你的 plan 去执行呢」把我打回，手工复算发现四束里**三束不达标**（R-B 4 / R-C 4 / R-D 3）。同一轮还连带漏了计划承诺的 criteria 落盘、handoff、PROGRESS.md 更新——**PROGRESS.md 停在两周前的旧轮次，会直接误导下一个 session**。**判据：bash 断言只能证明"文件存在、格式合法"，永远不能替代 Phase 的阶段门控；两者必须分别验、且门控结果要落盘。**凡计划里出现"每 X ≥ N 条"这类**按分组计数**的门，交付前必须逐组算一遍贴出来，不许用全局总数代偿（总数达标而分组不达标是常态，不是意外）。附带教训：计划本身也会漏产出物（该轮计划就没列 handoff），所以收尾时要把「计划承诺过什么」和「实际产出了什么」两张清单对一遍，不能只顺着自己记得的做。关联 feedback_recheck-completeness-claims-against-sources。

**延伸（2026-07-24，多态 UI 只验了第一态就交付）：** 给语音输入胶囊做动效重设计，我写完 HTML/CSS/JS 预览稿**一次没跑**就推给 luca，他一眼看出「结果态没有了…你认真检查一下你的 ui 生命周期」。逐帧查出 3 个真 bug，其中主因是个可复用的 CSS 机理：**元素可见性若靠入场动画的 `fill: forwards` 维持（基线 `opacity:0`），任何后续同特异性动画（成功 pulse / 错误 shake）都会整体覆盖它，且新动画 keyframes 不写 opacity 就回落到 0 → 后续状态里整个组件消失**；同族第二坑是入场 stagger 规则（`.pill.enter > *`）特异性高于后出现元素自己的显形规则，把入场结束后才出现的 ✓/文案永久压在 opacity 0（入场那轮动画早跑完不会重放）。修法：可见性改**显式状态类**、入场类改**瞬时类**（跑完就摘）。**判据：带状态机的 UI（HUD/多态组件/动效原型），「首帧渲染对」≠ 验过——必须驱动跑完每一个状态并逐态取证；静态读码和看第一帧结构性地看不出上述覆盖问题。** 本次奏效的取证法：起本地 http server（`file://` 被浏览器工具拒），用 `javascript_tool` **确定性摆状态**（页面若有自动循环先 `window.playAll=()=>{}` 掐掉，否则它和你的手动驱动抢同一组件、读数全是竞态垃圾），再 `zoom` 截图看像素。**陷阱：后台/未聚焦标签页会节流样式推进，`getComputedStyle` 读到的动画中间值是假的（我读到 `opacity:0`、宽度不变，差点据此判"没修好"）——截图会强制渲染，以像素为准、不以计算样式为准。** 关联 [[feedback_1to1-ui-replication-method]]（交付 1:1 而非近似）。

**延伸（2026-08-05，最贵的一次：证据不具区分性 = 假证据，我据此发过一个错的"已验证"commit）：**
把 luca_gstack 迁到 Codex，我要证明"我的 hook 在真实 Codex session 里跑起来了"。拿到的证据是
codex stderr 里的 `hook: UserPromptSubmit` / `Completed`，据此写进 commit「hook 确实在真实
session 中被调用」。**独立评审证明该行由 `~/.codex/hooks.json` 里既存的第三方 hook 产生**——
在一个完全没有 luca_gstack 配置的空目录里也照样打印。我用自建 marker 复验：仓库级 hook
**一次都没执行过**（codex 0.146.0 实测根本不加载仓库级 hooks.json）。整套接线是死的，而我
声称验过了。同一个判据我返工了三版才对：v1 用 `.session-*` 文件差集（取决于模型这轮是否恰好
调工具，同一接线两次跑出**相反**结论）→ v2 用上述泛化 `hook:` 行（任何 hook 都会打印）→
v3 才换成**只有本仓 hook 才可能产生的副作用**。
**这正是本文早已写下的"区分性用例"原则**，只是形态换成了"正向信号"就没认出来：
测试全绿容易怀疑，**一条看起来正中靶心的正向日志**几乎不会被怀疑。

**判据（本次新增，可直接套用）：把每条证据过一遍反事实——**
> **「如果我做的东西根本不存在／完全没生效，这条证据还会不会出现？」会 → 它不是证据。**

具体做法：能造**阴性对照**就造（本次决定性的一步是把同样命令跑在一个没有我的配置的空目录里，
一秒看穿）；证据优先选**只有我的东西才可能产生的副作用**（自建唯一 marker、专属日志、
带我方标识的字段），不选任何共享/泛化通道（通用日志行、退出码、"没报错"、公共计数器）。
同族反例见本文开头的"全 idle 假绿"——那是阴性侧的同一个病：**所有用例返回同一个值**与
**这条信号在我不存在时也出现**，都是"信号无区分度"。

**延伸（2026-08-05，输入侧的同一个病：夹具形状是想象的，测试就在测不存在的场景）：**
上一条讲**证据**要有区分度；这条讲**输入**。同一场 Codex 迁移里，我的集成测试给 `apply_patch`
工具喂 `tool_input={file_path:...}`——照 Claude Code 的形状想当然写的。实测（下面的技法）才发现
Codex 传的是 `{command:...}`，**根本没有 file_path**。后果：测试 16 项全绿，却完全测不到
"工具名映射错了会让项目隔离静默失效"这个 BLOCKER——**因为它测的是一个 Codex 永远不会产生的场景**。
同一轮还有个更大的：matcher 我写 `^(shell|apply_patch)$`，而真实 tool_name 是 **`Bash`**，
PreToolUse 因此从不触发，项目隔离形同虚设、全程无声。
**病根**：我一直在验"行为对不对"，却从没验过"我喂进去的输入长得对不对"。
夹具形状本身就是一个**需要实测的事实**，不是可以照文档/照另一个平台想象出来的东西。

**技法（本次的解药，通用于一切 hook / 回调 / webhook / 插件接口）：**
**先用通配符抓一次真实载荷，再拿抓到的形状当夹具。** 具体做法：把 matcher 放宽到 `.*`
（或注册一个只 dump stdin 的探针 handler），跑一次真实流程，把 `{事件名, 工具名, 载荷键}`
落盘看一眼——本次两条命令就同时定死了 tool_name 与 tool_input 结构，而此前我据文档猜了两轮全错。
**判据：集成测试的输入若不是从真实系统抓来的，它的绿色不能作为"这条路径能用"的证据。**
交付前自问一句：**我这个测试喂进去的东西，真实系统真的会这样传吗？我见过吗？**

**延伸（2026-08-05，独立性是"假设"层面的，不是"代码分开写"层面的）：**
同一场 Codex 迁移里连中两次同一个病根：

- **"独立交叉校验"其实不独立。** 我给 schema 归一化写了个校验器，还特意注释「独立实现同规则，
  不复用被测函数，避免自证」——但它和实现犯了**完全一样的假设**：都只在节点有 `properties`
  时才检查。于是真实 schema 里那个自由形态 object（`{type:'object'}` 无 properties）双方都放过，
  **交叉校验报告 0 处违规，真实 API 却 400**，整个 workflow 每次第一步死掉。
  代码是分开写的，盲点是共享的 → 它不是第二双眼睛，只是同一个错误的第二份拷贝。
- **测试夹具的实现技术也会掩盖缺陷。** 我的假 codex 用 Node 写 stdout，而 Node 会自动 drain 管道，
  于是"管道无人消费会挂死"这个真缺陷在测试里复现不出来——把被测代码改回有缺陷的版本，
  测试**照样全绿**（变异测试当场抓出，是个安慰剂断言）。改成 shell(dd) 阻塞写、
  且让它**先写 stdout 再写结果文件**（阻塞则结果永远写不出），断言才有区分度：变异后挂死 15030ms 转红。

**判据：** 问"这个校验/夹具**独立于什么**"。独立于*代码*没用，要独立于**假设**与**实现技术**。
具体三问：① 我的校验器和被测实现，会不会因为同一个想当然而同时放过某类输入？
② 我的夹具用的技术栈，会不会自带缓冲/重试/容错，把要测的失效吃掉？
③ 派独立 reviewer 时，我给它的前提里有没有夹带我自己的错误假设（给错前提，再独立的人也得出同样的错）。
**验独立性的唯一硬手段仍是变异测试**——把缺陷放回去，看校验/夹具/reviewer 是否真的转红；
不转红就说明它守的是我的假设，不是真相。

**（2026-08-05 续，变异测试自身也会太弱）** 我给刚放宽的 hedge 门做过变异并确认转红，
但我的变异是**粗暴删除**（抽掉实测锚）；独立评审的变异是**精巧绕过**——写一句
「effort 一律固定、调用方不可配置」，词表全中、与磁盘上 `.codex/agents/*.toml` 的三档并存
直接矛盾——**22/22 全绿**。同轮另外 5 处同型：`-C AGENT_CWD` 原句保留、末尾再追加一个
`-C ROOT`（grep 照常命中，末次生效值却是仓库根）；matcher 写 `^(Bash|apply_patch)$never`
（含全部关键词但永不匹配）。
**判据：变异要模拟"聪明的改坏"，不只是"删掉修复"。** 三种必须覆盖的形态：
①**删除**（最容易想到，也最弱）②**追加**——保留原句、在后面加一条覆盖它的（grep 型断言全瞎）
③**同义替换**——满足断言的字面要求但语义相反（词表型断言全瞎）。
配套的结构性对策：**断言查"最终生效值"而非"源码里有没有这个字符串"**
（本次把 grep 改成让假二进制落盘真实 argv、查末次 `-C`/`-s` 值），
以及**禁令清单不能和被查对象放同一个文件**（自证式门禁改一处就一起松，
本次把实测禁用值硬编码进检查脚本侧）。

**延伸（2026-08-06，"穷尽矩阵"全格失败 = 可能是一次失败被复制了 N 遍；缺的是阳性对照）：**
判断 Codex 是否加载仓库级 hooks.json，我跑了**三个位置 × 三种 trust 状态 = 九格**矩阵，
全部不触发，据此下了**架构级结论**（"仓库级不被加载"）并按它重建了整套注册方式。
独立评审一句话推翻：hooks.json 顶层**只接受 `description` 与 `hooks`**，而我写了个 `_comment` 键，
整份文件被拒（`unknown field _comment`）。**九个格子每一格都因这同一个键失败**——
我测的从来不是加载机制，是我自己写坏的那一个键。改名后 `hooks/list` 立刻 7 条 → 13 条。
更难堪的是：那行警告**每次启动都在打印**，我跑了几十次 codex 一次都没读过它；
而被这个假前提固化的地方有四处，**包括我自己写的回归断言**——等于把错误架构钉成了测试。

**判据（本次最贵的一条）：**
> **矩阵/多用例实验里，如果"所有格子都失败"，先怀疑共因，别急着下结论。
> 全负结果证明不了被测对象不行——它同样符合"我的实验装置坏了"。**

对策三条：
1. **必须有阳性对照**：至少让一格**成功**，证明装置真的能工作。做不出任何一格成功时，
   结论只能是"我没能让它工作"，不能升格成"它不支持"。
   （上一条记的是**阴性**对照——同样的东西在没有我的配置时也出现；这条是**阳性**侧，两者都要。）
2. **共享变量要拆开**：所有格子若共用同一份我方产物（配置文件、夹具、脚本），
   那不是 N 次独立实验，是同一次实验的 N 个副本。先把那个共享物本身验证一遍。
3. **启动期 warning 是一等证据源**：它一闪而过、不进日志、不影响退出码，最容易被跳过；
   自动化调用时**主动 grep stderr 里的 warning/error**，别只看退出码与最终输出。
   本次真相就明写在那行警告里，代价是一整套错架构。

同族：把"我没找完"包装成"客观无解"见 [[omission-as-disguised-avoidance]]——
这次是它的实验版：**把"我的装置坏了"包装成"这个机制不存在"**。

---

## 并入：feedback_ui-entry-discoverability（2026-07-02 治理合并，内容原样保留）

给 Muse 接 AI HOT「日报」时，我把它做成侧栏第 9 个挤窄 tab、且侧栏默认收起。渲染/逻辑哨兵全绿（渲染对、XSS 挡、点 tab 能激活面板），我就当作做完。但用户**连着两次找不到**（"没看到侧栏" / "哪里有"）——绿哨兵验了"**能渲染**"，却没验"**用户够得着/找得到**"。

**How to apply:** 加用户面入口要过**可达性自检**：① 承载容器默认可见吗（默认收起/折叠？）② 新项是否被挤成最后/最窄/最不显眼 ③ 主动想要的功能是否给了显眼位（开场卡片/默认展开/置顶）而非埋进 tab 海。交付前用"一个没看过的人能在 5 秒内找到并看到吗"反问一次。

与 [[feedback_verify-your-verification]] 同源——"目标"应含可见性与可发现性，不止 DOM/class（渲染现实哨兵那一面已并入该条）。

---

## 并入：feedback_reproduce-real-env-not-approximation（2026-07-10 治理合并，内容原样保留）

---
name: feedback_reproduce-real-env-not-approximation
description: 复现故障定位根因时逐字照搬真实进程的 env/args，别手工剥近似——漏一个变量就会得出自信的错根因
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 5be0f3e6-d4b8-4c07-90e9-e06d7a30ee2b
---

定位一个故障的根因时，我没照搬故障进程的**真实环境/参数**，而凭印象手工剥了个"近似"环境来复现——`env -i HOME=$HOME PATH=$PATH claude -p`——漏了真实系统实际会传的 `USER`。少了 USER，本机 claude 回退到耗尽的 API 额度报「Credit balance is too low」；我据此自信判定"额度耗尽=根因"，还顺着改了 6 个文件的流程/记忆/规则。后来把真实 spawn 的 26 个环境变量抓出来一比对，发现 OD 本就传了 USER（鉴权正常），credit 错误纯是我复现漏变量的产物——真因是别的（生成慢 + daemon SIGTERM 重启）。

**Why:** "复现"只有在**与真实条件一致**时才有诊断力。手搓的近似环境少一个变量 / 少一个 flag / 错一个 cwd，就可能触发真实系统根本不走的代码路径，给出一个**看起来铁证、实则全错**的根因；而且越自信越危险——我会据它继续造一堆下游改动，再全数返工。这是 over-claim 的一个具体机理。

**How to apply:** 要复现某进程的故障来定位根因，**先抓它的真实调用**——`ps -Eww` / `ps -o args` 拿完整 argv，连 env 一起（`ps eww` 或从 spawn 日志），再**逐字照搬**那套 env+args 复现，不凭记忆剥近似。尤其改"鉴权 / 计费 / 路径解析"这类对环境敏感的结论前，必须确认复现环境 == 真实环境（差异变量逐个排除）。把"我的复现和真实调用差哪些变量"当成红队第一问。关联 [[feedback_redteam-own-analysis-before-shipping]]（上报前红队自己结论）、[[feedback_verify-your-verification]]（信绿前确认脚本真考验目标）、[[feedback_systematic-not-whackamole]]。

---

## 并入：feedback_verify-in-the-deploy-scope（2026-07-17 治理合并，配置/开关验证切面）

**验证一个将要部署的配置/开关时，实验用的注入通道必须就是部署用的那条。** 在有优先级链
（`a ?? b ?? c` / `||` / first-wins）的系统里，**测高优先级分支、部署低优先级分支 = 实验结构上
无法证伪命题**——低优先级分支完全不工作，实验照样 PASS。

**Why:** 2026-07-16 person 记忆方案，我把「`autoMemoryDirectory` 真机生效」（后果栏写「全案作废」）
标为已清，证据是真机 `claude -p --settings <tmp.json>` 双向 CANARY 通。红队反编译出解析函数：
`--settings` 命中的是 `flagSettings`（`??` 链第 2 位），方案要部署的 `~/.claude/settings.json`
是 `userSettings`（最后一位）；按 `??` 短路，flagSettings 命中时**根本不求值 userSettings 分支**——
我的 CANARY 只证明了 flagSettings 通，一文不值。这是 feedback_reproduce-real-env-not-approximation
（少一个 flag→走真实系统不走的路径）在"验证配置"上的等价物。

**How to apply:** 声称「已验证」前多问一句 **「我这个实验，在命题为假时会失败吗？」**。配置/开关类固定动作：
① 先把**解析顺序**读出来（优先级链 / 短路语义 / 记忆化都要看）；② 确认**实验注入的那一层 == 部署要写的
那一层**，不同层→实验作废重做；③ 记忆化的 key（如 `${rc()}|${CPe()}`）会让同 session 改 settings 不重算，
必须**新 session** 验；④ 变异测试在这里的等价物 = **把高优先级分支拿掉，看低优先级分支是否仍然生效**。

---

**8-08 新形态（比"变异太弱"更靠前的一层）：shim 掉边界 = 那一侧的代码从未被执行 = 变异它永远不会转红。**

muse `sidebar_selection`：我把 `rendererRequest` 整个 shim 掉，用**手写的、已消毒的** fixture
喂被测分支，然后报出 28/28 + 变异 11/11 当证据。独立评审官拿 6 个真缺陷变异打它——
**全部全绿**，包括"把消毒器整个不接线"（`text: selCleanText(...)` → `text: r.text`）。
根因不是断言写得松，是**抓取脚本与消毒接线一次都没被执行过**；对从未执行的代码做变异，
自然不会转红。而攻击者真正所在的位置恰恰就是我 shim 掉的那道边界（页面 → 抓取脚本），
真 BLOCKER（页面能整份伪造"用户选中了什么"）就活在那里。

**诊断（写测试时就问，别等评审官）：**
- **"我的套件里，被测的那个函数真的被执行过吗？"** 纯函数测得再细，也证明不了它**被接上了**。
- 我 shim 掉的是哪道边界？**攻击者/真实故障在边界的哪一侧？** 同侧才有意义，异侧等于没测。
- 变异清单里有没有一条是"把 A 接到 B 的那行删掉"？没有 → 接线是盲区。
- 结构性对策：**一份离线套件 + 一份真环境 e2e**，并在离线套件顶部写明"我测不到什么、归谁测"。

**同轮第二课：夹具不够 ≠ 代码有错。** 三次假失败全是夹具问题——敌意页把自己的选区脚本
也搞死了、SVG 夹具的驼峰元素根本不在祖先链上、只有一张卡时最短选择器本就唯一（代码返回
的是对的）。**断言红了先问"这个夹具真的触发了我要测的性质吗"**，别急着改代码去迁就；
反过来断言绿了也要问同一句（后台页签定时器被节流到约每分钟一次，把 40ms 轮询探针饿死，
于是变异漏网——探针得先激活页签才有区分度）。

**8-13 补上结构性对策：夹具需要"具备某性质"时，就在测试里把那条性质也断言一遍。**
光靠"我算过了"不行——算错了没人告诉你，真断言就变成空转。两个实证同日发生：
① 要让一行**横跨 64KB 块边界**去测跨块 carry，我把 `metaLine(n)` 的 n（填充**字符数**）
当成了行**字节数**，那行根本没骑上边界 → "丢掉 carry"的变异照样全绿。补一条
`J3b0 [夹具自检]`（断言目标行起点 < 65536 < 行尾）之后，变异立刻转红。
② 阳性对照本身失败：给 `<webview>` 赋 `src` 是 **embedder 发起**的导航，
和 `loadURL` 一样**不触发 `will-navigate`** —— 我以为在测守卫，其实一条事件都没产生。
**判据**：凡夹具的价值依赖某个"我算出来/我以为"的性质（跨边界、超阈值、有重复、
键是多位数、目标行在第 N 块），就多写一条断言把那个性质本身钉住；它红了说明**装置**坏了，
与被测代码无关，两类失败必须能一眼分开。同族的"全格失败先怀疑共因"见下文 8-06 条。

**同日第三课（形状不同，最阴）：「更强的断言」未必更有牙口——先问"让它红的那个世界，
在我的装置里存在吗？"** 独立评审官正确指出我的 e2e 断的是 `ev.defaultPrevented`（中间标志，
只证明"我调了 preventDefault"，证不了 Chromium 真停住了导航），要我改断**用户可见的最终 URL**。
我照做了，看着严格得多——**结果零牙口**：沙箱连不上真 x.com，跨分区那一跳必然加载失败、
页签停在原地，于是"最终 URL 不是 x.com"这条在**守卫开不开都成立**。
这不是夹具算错（上一条），是**我要断言的那个坏结局在装置里根本不可能发生**，
断言只能恒绿。解法是让坏路径**真的走得通**再断言（本例：Chromium `--host-resolver-rules`
把真实域映射到本地服务器，那一跳真能加载成功）——改完 H1a 变异立刻转红、页签实实在在落在了 x.com 上。
**通用问法：这条断言要红，需要世界上发生什么？那件事我的装置供得起吗（网络/权限/时序/真实域名）？
供不起就是装饰品。** 采纳别人的口径升级同样要过这一关——口径对，不代表落地那版有牙口，
一律用变异复验（本例正是变异当场拆穿的）。

**8-12 同一族第二次（这次是被点名之后又犯）：源码 grep 断言冒充行为断言。**

上一个增量里独立验证官已经点过名——「`test-selection.mjs` 的 C0/C0d 是对源码字符串做 grep，
今天的码是对的，风险在于明天被拆掉时没人会发现」，还给了实证（把渲染层改成忽略 readOnly，
main.js 的修复还在、断言照绿、缺陷逐字复现）。**同一 session 我又写了一条一模一样的**：
`ok('按 id 合并', /byId\.set\(meta\.sessionId/.test(seg))` —— 变异只要把 key 改成
`meta.sessionId + ':' + file` 就恢复了「一个文件一行」的缺陷，而那句字符串还在，断言全绿。

同日还有一个**手抄**变体：为验"面板 partition 必须等于 partitionOfHost(其 src 域)"，我在测试里
照着实现重写了一遍判定，只搬了 `===` 那半、漏了 `endsWith`，把**正确的** `www.youtube.com`
判成红（假失败，差点去"修"没坏的代码）。

**判据（写断言时就问，别等评审官）：**
- 这条断言查的是**源码长什么样**还是**跑出来什么结果**？前者只在"删掉"型变异下转红，
  对"改写/等价绕过"型完全瞎。
- 我是不是在测试里**重写了一遍被测逻辑**？重写就会漂移——切真函数来跑（`new Function(切片)`），
  或造夹具起子进程跑真实现。手抄的那份出错时，红的是测试不是代码。
- 实在只能查源码时（如跨文件不变量、死代码的前置条件），**明写它是源码级断言**，
  并把它守不住的那部分交给另一条行为断言（本轮：J1e 守不住的死代码，由 J1g 钉住其降序前提）。

## 8-15 · 空输出有两种成因，而它们长得一模一样

macOS 没有 GNU coreutils 的 `timeout`。我连写四条 `timeout 60 grep -a ... 二进制`
去找 claude 的限额文案，**每一条都是 `command not found`、一次都没跑**，终端只回给我空白。
我几乎据此写下"claude 二进制里没有限额相关字符串"这个**阴性结论**——去掉 `timeout` 重跑，
`Do you want to proceed` / `Enter to select` / `Yes, and bypass permissions` 一大把。

**判据**：拿到空输出要先问「这是**没匹配到**，还是**命令根本没执行**？」——
终端上两者一模一样，而后者会把"我没测成"伪装成"它不存在"。
这是 8-06「全格失败先怀疑共因」的最小形态：**装置坏了和结论为负，产生同一个观测**。

**三条对策**：
- **阴性断言前先做阳性对照**：同一条命令换个必然有输出的模式跑一次（`grep -c . <file>`）。
  没有阳性对照的阴性结论，只能说"我没能找到"，不能升格成"它没有"。
- **stderr 是一等证据**：`command not found` 只走 stderr，被"无输出"完全掩盖。
  自动化里主动看 stderr，别只看 stdout 和退出码。
- **macOS 上别默认 GNU 工具存在**：`timeout`/`sed -i`/`date -d` 都不是 BSD 自带；
  要超时就用语言内置（`execFile` 的 timeout 选项）或 `gtimeout`，别写进一次性排查命令里。

（同族：本条讲**阴性**证据的成因歧义，8-05 那条讲**阳性**证据的区分度，两面同一枚硬币。）

## 索引原文存档（2026-08-15，G5 瘦索引前的完整索引行，逐字保留）

- [验证铁律](feedback_verify-your-verification.md) — 宣称修好/done前必须跑项目现有测试套件(bats/swift/pytest)真绿，别只靠临时合成；**带状态机的UI(HUD/多态组件/动效稿)「首帧渲染对」≠验过——必须驱动跑完每个状态逐态截图取证；可见性靠动画fill-forwards维持会被后续动画整体覆盖成隐形，静态读码看不出；后台标签页节流让getComputedStyle读出假值，以像素为准(7-24 胶囊结果态缺失被luca当场看出)**；信"绿/一致"前先确认脚本真考验目标(fixture存在+区分性用例+变异测试)；**8-05 最贵一次：正向证据同样要有区分度**——拿 codex stderr 的 `hook:` 行当"我的hook跑了"写进commit，实为既存第三方hook所打印(空目录里照样出现)，真相是仓库级hook一次都没执行过；同一判据返工三版才对。**反事实自检：「我做的东西如果根本不存在/没生效，这条证据还会不会出现？」会→不是证据**；能造阴性对照就造(同命令跑没我配置的空目录，一秒看穿)，证据只选**我方专属副作用**(唯一marker/专属日志/带我方标识字段)，不选共享泛化通道(通用日志行/退出码/"没报错")；**同日输入侧同病**：给 codex 的 apply_patch 喂想象的 file_path 载荷(真实是 command)、matcher 写 shell(真实 tool_name 是 Bash)→测试16项全绿却测的是永不存在的场景，项目隔离静默失效全程无声。**夹具形状本身是需实测的事实**，不能照文档/照另一平台想象；解药技法=**先用 matcher `.*`(或只dump stdin的探针)抓一次真实载荷**再拿它当夹具(两条命令定死tool_name+载荷结构，此前据文档猜两轮全错)；自问"我喂进去的东西真实系统真的会这样传吗？我见过吗？"；**8-05 再进一层：独立性是"假设"层面的不是"代码分开写"层面的**——我写的"独立交叉校验"(还特意注明不复用被测函数)与实现共享同一假设(都只在有properties时才查)→对真实schema报0违规而API 400；假codex用Node写stdout(Node自动drain)让"管道挂死"缺陷复现不出来、改回缺陷版照样全绿(安慰剂断言)。三问：①校验器与实现会不会因同一个想当然同时放过某类输入②夹具技术栈自带的缓冲/重试会不会吃掉要测的失效③给独立reviewer的前提里有没有夹带我的错误假设(给错前提再独立的人也得同样的错)；**验独立性唯一硬手段是变异测试**——把缺陷放回去看是否真转红，不转红说明它守的是我的假设不是真相；**但变异本身也会太弱**：我只做"粗暴删除"型变异并确认转红，评审用"精巧绕过"型（词表全中却与磁盘矛盾的假话／保留原句再追加一条覆盖它的 `-C ROOT`／matcher 写 `^(...)$never` 含全关键词但永不匹配）拿到 22/22 全绿。**变异要覆盖三形态：①删除(最弱)②追加(破 grep 型断言)③同义替换(破词表型断言)**；结构性对策=断言查**最终生效值**而非源码字符串(让假二进制落盘真实 argv 查末次值)、**禁令清单不能与被查对象同文件**(自证式门禁改一处一起松，改为硬编码进检查脚本侧)；**8-06 最贵一条：「穷尽矩阵」全格失败 = 可能是一次失败被复制 N 遍**——我跑三位置×三trust=九格全不触发，据此下架构级结论"仓库级hooks不被加载"，真因是九格共用的那份配置里我写了个 `_comment` 键(顶层只接受 description/hooks)导致整份被拒，改名后 hooks/list 立刻 7→13 条；且那行警告每次启动都在打印我几十次没读过，假前提还固化进了我自己的回归断言。**判据：全格失败先怀疑共因——全负结果同样符合"我的实验装置坏了"**；三对策=①必须有**阳性对照**(至少一格成功证明装置能工作，做不出就只能说"我没能让它工作"不能升格成"它不支持")②共享变量拆开(所有格子共用我方产物就不是N次独立实验)③**启动期 warning 是一等证据源**(不进日志不影响退出码最易跳过，自动化时主动 grep stderr)；非平凡重构/流程改动要端到端模拟跑+独立对抗review，grep/读只验表面验不出语义/执行缺陷；加用户面入口除了验"能渲染"还要验"够得着/找得到"；**验证者必须独立于修复者**——luca明确要求"测试不能自己测"，13项修复逐项派不知情独立agent核验(只给原始finding+现状,不告知怎么改的)，抓出2处自己回读代码不会主动怀疑的真缺口(同一问题的第二条触发路径未堵、文档局部同步遗漏)(7-03)；**计划里有两套验收时别用容易的冒充另一套**——bash 断言（文件存在/总数/语法）永远替代不了 Phase 阶段门控（「每束≥5条」这类分组计数门），交付前逐组算一遍贴出来，全局总数达标而分组不达标是常态；收尾还要把「计划承诺了什么」和「实际产出了什么」对一遍(7-22 只跑A3全文≥20URL就宣布完成,实际4束里3束不达标,并漏了criteria/handoff/PROGRESS)；**8-08 更靠前的一层：shim 掉边界＝那侧代码从未执行＝变异它永不转红**——我 shim 掉 rendererRequest 后报 28/28+变异 11/11，评审官 6 个真缺陷变异(含"消毒器整个不接线")全绿，真 BLOCKER 就活在我 shim 掉的那道边界上；诊断＝"被测函数真的被执行过吗/攻击者在边界哪一侧/变异清单里有没有『删掉接线那行』"，对策＝离线套件+真环境 e2e 并在离线套件顶部写明测不到什么；**夹具不够≠代码有错**，断言红了先问"夹具真触发了我要测的性质吗"(三次假失败全是夹具)，绿了也要问(后台页签定时器节流饿死探针→变异漏网)；**8-12 同族第二次(被点名后又犯)：源码 grep 断言冒充行为断言**——`/byId\.set\(meta\.sessionId/` 这种只在"删掉"型变异下转红，改写成 `sessionId+':'+file` 恢复缺陷它照绿；同日另一变体=在测试里**手抄**被测逻辑(只搬 === 漏了 endsWith)把正确代码判成红。三问：查源码长相还是跑出来的结果/有没有重写一遍被测逻辑(该切真函数或起子进程跑真实现)/只能查源码时明写它是源码级并把守不住的部分交给另一条行为断言**别信子进程/worker 自报成功**——exitCode 0 + 像样的成功JSON 也可能文件落错目录，用外部 grep/独立读盘核实(7-06 muse fork)（并入 [[feedback_run-tests-before-claiming-done]]、[[feedback_systematic-not-whackamole]]、feedback_ui-entry-discoverability、[[verify-with-real-evidence-before-reporting]]、feedback_reproduce-real-env-not-approximation：复现故障定根因要 ps -Eww 逐字照搬真实 env/args 别手剥近似，漏一个变量就得出自信的错根因并连锁返工(6-16)；verify-in-the-deploy-scope：验配置/开关时注入通道必须==部署通道，测高优先级分支拿掉后低优先级是否仍生效——`--settings`命中`flagSettings`根本不求值`userSettings`分支,CANARY假绿(7-16)）
