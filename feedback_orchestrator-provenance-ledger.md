---
name: orchestrator-provenance-ledger
description: spawn 以共享状态为证据基底的实验/评审 agent 前，把编排 session 自己的带外写入做成台账内联喂入；agent 在飞期间冻结新写入——清洁上下文 ≠ 干净基底
metadata: 
  node_type: memory
  type: feedback
  originSessionId: e9ed9da7-ac6e-4acf-8833-9f8b8ec59865
  modified: 2026-08-05T02:25:32.834Z
---

**spawn 任何以「共享状态」为证据基底的实验/评审 agent 前，先把编排 session 自己对该状态的
全部带外写入做成台账（路径+时间+动作+归属），作为一等输入内联进 prompt；agent 在飞期间
冻结对该基底的一切新写入。**

**Why:** 2026-07-16 person 记忆共识循环 3 轮触顶未收敛，R3 两条致命 + 两条严重全部源于
同一结构缺陷：编排 session 当日往两个 person store 写了 10 个文件，而清洁上下文的方案作者
不知情，把「编排者当天造的状态」当 found state 立论——红队用 mtime/transcript 取证击杀，
每一轮都在为编排者的带外行为买单。为防叙事污染设计的清洁上下文，反而制造了结构性失明。
**R4 把台账内联 + 在飞冻结后，A 类事实缺陷 5/5 视角逐条真机核验确认全部消失——修法有效性
已实证。** 期间两次差点破戒（Stop hook 提取裁决、R4 补跑前），均靠显式推迟守住。

**在飞期间的隔离是双向的（2026-08-04 补，险情实证）：** 上面写的是「我不写它的基底」。
**反向同样成立且更容易漏：它也在写我要验证的对象。** 派了做 mutation / 改代码类实验的 agent 后，
编排者**在同一工作区跑的任何验证都不可信**——不只是不该改，是不该**读来下结论**。
本次实证：订阅恢复后我立刻在工作区跑活体验收，拿到 2 个 FAIL，而当时 reviewer 正把
`decision:block` 翻译删掉做 mutation（diff 里带 `[MUTATION M2]` 标记）。那 2 个 FAIL 全是它造的。
**险在**：若没瞥见那行标记，我会把 mutation 的产物当真缺陷去"修"，很可能改坏一段本来正确的
翻译逻辑；反向也一样危险——把污染下的 PASS 当成完整通过。讽刺的是我上一轮刚显式守住了
"不改它的基底"，却没意识到自己转头跑的验证正踩同一个坑：**我守了写侧，漏了读侧。**

**How to apply:**
- spawn 前自问：「这个 agent 的证据基底里，有没有我自己写进去的东西？」有 → 台账内联进
  prompt。给路径指针不算（R1 作者确实读全了三份文件仍然失明——听话不是结构保证）。
- agent 在飞期间确需写基底 → 显式推迟成待办，episode/checkpoint 留明细，agent 落地后统一补。
- 台账本身的写入也要登记（递归适用）。
- **派出会改工作区的 agent（mutation / 改码实验）后，编排者不在同一工作区跑验证**。三条出路：
  ①等它落地再验（默认）②要并行就在独立 worktree / 副本上验 ③非验不可 → 先 `git diff` 确认
  工作区干净，拿到结果后**再 diff 一次**确认期间没被改过，两次都干净结论才作数。
- 拿到意外的 FAIL/PASS 时，**先问「这是被审对象的问题，还是我的观测被污染了」再动手**——
  归因到代码之前先归因到并发。

关联 [[missing-vote-is-not-a-verdict]]（同场循环的交付纪律）、
[[disclosure-is-not-remediation]]（登记了不等于处理了——台账是喂给实验的输入，不是免责声明）。

## 索引原文存档（2026-08-15，G5 瘦索引前的完整索引行，逐字保留）

- [编排前把自己的带外写入做台账](feedback_orchestrator-provenance-ledger.md) — spawn 以「共享状态」为证据基底的实验/评审 agent 前,先把编排 session 自己对该状态的全部带外写入(路径+时间+动作+归属)做成台账、一等输入内联进 prompt,agent 在飞期间冻结对该基底的新写入;清洁上下文≠干净基底,给路径指针不算(作者读全三份文件仍失明——听话不是结构保证)(7-16 共识循环3轮触顶,致命缺陷全源于编排者当天写的10文件被当found state立论,R4台账内联+在飞冻结后5/5视角事实缺陷清零)；**8-04 补对偶面：在飞隔离是双向的,写侧守住≠读侧安全**——派了 mutation/改码类 agent 后编排者在同一工作区跑的验证一律不可信(实证:订阅恢复后立刻跑活体验收拿到2个FAIL,全是 reviewer 当时正在做的 [MUTATION M2] 造的;差点把 mutation 产物当真缺陷去"修"、改坏一段本来正确的逻辑,反向把污染下的PASS当通过同样危险);三条出路:①等它落地再验(默认)②并行就在独立worktree验③非验不可则前后各 git diff 一次、两次都干净才作数;**拿到意外FAIL/PASS先问"是被审对象的问题还是我的观测被污染了",归因到代码之前先归因到并发**
