---
name: extracted-module-must-verify-wiring
description: 抽出共享模块后必须核实生产调用方真接线了——模块+其单测全绿≠功能生效，测的可能是死代码
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 0961ecdf-bcba-4f92-a28f-51d34cb10848
  modified: 2026-07-28T02:42:35.904Z
---

抽取/新建共享模块（resolver、helper、lib）后，**在宣称修好之前先 grep 生产调用点**，确认旧的内联实现
真的被替换掉了。模块自己的单测/parity 测试全绿**不构成功能生效的证据**——如果生产代码仍走老路径，
测的就是死代码，绿灯是假绿。

**Why:** 2026-07-24 跨-agent 适配 P1：我写了 `_memroot.py` + `memroot.mjs` 双侧 resolver 和 6 条
cross-lang parity 测试，全绿并据此提交、宣布"FIX-1 构造性成立"。9-agent 深审第一条就打回：10 个
实际读写记忆的 py 脚本仍是裸 `os.environ.get("MEMORY_ROOT", parents[2])`，**resolve_memory_root
只被 __main__ 和测试调用**=生产死代码，parity 测试因此是覆盖假象；云端态 py 侧照样落错根。返工：
补接 10 个脚本 → 触发沙箱逃逸回归 → 再修哨兵语义 → 再修 import shim 崩溃。

**同 session 内复发（2026-07-25，P2）→ 补一条更锐的：测试要有「判别力」，mutation 要覆盖每一个被声称的点。**
P2 我确实写了 wiring 集成测试、也做了 mutation——但只注入了 1 个站点的 mutant，看它变红就在 commit 里
写下"测试真有杀伤力，非假绿"。深审实测：4 个站点里 **3 个的 mutant 存活**。根因是 **fixture 无判别力**：
路径 `<tmp>/项目/muse/lucagstack/docs` 下，正确实现（canonical）与错误实现（旧首段正则）**返回同一个值
"muse"**，断言自然分不出。修法是造**判别式** fixture——把根换成不含 `项目` 二字的路径 + env 覆盖，
于是旧实现返回 ''、正确实现返回哨兵名，4 个 mutant 全部可杀（1/4 → 4/4）。

**同 session 第三次复发（2026-07-25，P0 harness）→ 新增量：延后接线必须如实标注。**
这次接线本身**按计划就该延后**（属 P4），所以"没接线"不是错——错的是**文档描述了一个尚不存在的
接线**：模块头注写"调用点：X 和 Y"（X/Y 从不 import 它）、CI 门名写"强制动词安全默认"（运行时
无此不变量）、commit 标题写"报告降级面而非静默失效"（不发生）。于是门 100% 永绿、守着空气。
修法不是改文案而是**真接线**（两个 emitter 出口都过门），并补 spawn 真 hook 的端到端断言。
副产物：接线时发现我原本的降级方向是错的——"unknown 也关强制动词"会在 CLAUDE_PROJECT_DIR
被剥的 Claude 会话里**静默丢掉主路径治理强制**，正确方向是"仅正向确定是 Codex 才降级"。
**接线会逼你面对真实语义；只写模块不接线，错误方向可以一直藏着。**

**第四次复发（2026-07-28，审计 Round2 抓 WS-B4 门）→ 新角度：回归门别测"生产契约不可能发生"的失败。**
我给 CR0044 补的 WS-B4 回归门，fixture 让 get_memory.py `raise ModuleNotFoundError('yaml')`，断言
session-restore 分类"缺 PyYAML"——门绿、mutation 也有杀伤力。但换分区审计一跑真实脚本就发现：真
get_memory.py 用 `try/except ImportError` **吞掉**缺 yaml 并静默降级、`--summary` 退出 0，故"缺
PyYAML"分类分支在真实缺-yaml 环境**永不触发**。门测的是一个生产契约下不可能发生的失败 → 保护死
路径、给假信心（让人以为缺 yaml 会被提示，实际不会）。修：删死分支，门改测**真实可达**失败（python3
不在 PATH → "未找到 python3"）。**教训：写失败路径的回归门前，先跑真实依赖确认该失败真会传播到你
断言的地方——别照着分类代码反推一个 fixture 去喂它，那只证明"若发生则分类对"，不证明"会发生"。**

**How to apply:**
- **回归门测的失败必须在生产契约下真实可达**：写"X 失败 → 分类/告警 Y"的门前，跑一次真实依赖看 X
  会不会真传播过来（很多库把 ImportError/缺配置吞掉降级，你断言的 catch 分支根本进不去）。照分类
  代码反推 fixture = 只证"if 发生则处理对"，不证"会发生"——后者才是回归门的价值。
- **延后接线时，三处措辞必须一致地说"还没接"**：模块头注（写"尚无调用点，X 阶段接线"而不是
  "调用点：A/B"）、门名（写"模块自洽"而不是"运行时不变量"）、commit（写"探针就位，未接线"）。
  能立刻接就接——接线成本通常远低于"日后再对齐三处文档"的成本。
- **新建能力的门，至少有一条断言必须跑生产路径**（spawn 真 hook / 真调用方），否则门只证明
  模块内部自洽，不证明它在系统里生效。
- **判别力自检（写完每个断言就问）**：「如果实现是错的，这个断言的期望值会不会**恰好也成立**？」
  尤其当 fixture 的期望值能从路径/文件名里"顺出来"时，多半没判别力。让正确与错误实现**产出不同值**
  才叫测到。挑哨兵值（`zz-ident-probe` 这类绝不会从别处冒出的名字），别用 fixture 路径里已有的词。
- **mutation 要按「声称覆盖的点」逐个来**：测试说覆盖 N 个站点/分支，就注入 N 个 mutant，全 killed 才
  能说"有杀伤力"。**绝不能用 1 个 killed 泛化成整体有效**——这正是我犯的第二次错。
- 抽模块后固定做两步：① `grep -rn "<旧内联模式>"` 确认残留为 0；② `grep -rn "<新模块名>"` 确认调用
  点数量 == 预期替换点数量。两个数对不上就是没接线完。
- 给 wiring 本身写一条**集成**断言（不只测模块）：把 wiring 换回旧写法，该断言必须变红。用
  worktree 注入 mutation 实测一次，绿→红 才算测试有杀伤力。
- 同族既有教训：[[feedback_verify-your-verification]]（信"绿"前先确认脚本真考验目标）——本条是它
  的具体变体：**测试考验的对象可能压根没被生产调用**。
- 深审/mutation 的价值实证：这条是 review 维度①和 mutation M5 各自独立抓到的，靠自查没发现。
  大改动按 [[feedback_redteam-loop-then-deep-review-big-changes.md]] 走完实现后深审，不省。

**第四种形态（2026-08-15）——接线对了、测试对了，东西却在一条没合并的分支上。**
这次的断点不在 import 层，在**交付层**：我给记忆系统建了检测器、digest 四项改造、
person/project 检索层，逐项变异测试、verify.sh 74/0、commit + push 全绿，然后向 luca
报告"已交付"。实际上 5 个 commit 全躺在 `memory-governance-2026-08-15` 分支上，
**main 一行没有，他那个 session 跑的还是旧逻辑**。分支是当初为绕开另一个 agent 的在途
工作而开的（正确决定），但我把"绕行手段"当成了终点，忘了它还得回来。

讽刺的是这正是我那一整天在治的病：`supersedes` 读侧实现了却没有执行的口、
`SC-20260622-001` 有规则却没有执行面、7-16 那批方案做完就沉底——我批评了一天，
自己在最后一步犯了同一个。

**判据升级：「done」的最低标准不是 push，是「跑它的那个东西现在会执行到我的代码吗」。**
自问三句：①它在哪个 ref 上？②真实运行时读的是哪个 ref？③这两个是同一个吗？
分支/worktree/fork 这类隔离手段天然制造这个缺口——**开隔离的时候就把"合回去"写进待办**，
否则隔离会静默变成搁置。
