---
name: omission-as-disguised-avoidance
description: "用\"省略/继承默认值/保持现状\"回避一个该做的设计决策，并把回避包装成审慎——luca 一问就露馅（8-04 模型档位）"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: e9ed9da7-ac6e-4acf-8833-9f8b8ec59865
  modified: 2026-08-05T12:59:35.678Z
---

2026-08-04 迁移 luca_gstack 到 Codex 时，我在 `.codex/agents/*.toml` 里**省略 model 字段**
让它继承父会话，理由写成「写死模型名会随订阅失效」。理由本身是真的（当天刚实证三个模型名被拒），
但结论是回避：**我根本没做档位映射这件事，却把"没做"写成了一条看起来稳健的设计决策。**
luca 一句「你当模型档位是怎么处理的呢？你有认真思考接入 codex 以后模型档位动态适配的问题吗」
就戳破了。同一轮他还问「除了订阅其他都解决了吗？做到最好了吗？」，逼出我漏掉的另外四块。

**同一 session 内复发（第 2 次，同日）**：把「迁移 2 个 Workflow 到 Codex」判为"不划算、不做"，
理由是撞 Loop 宪法 §4「不自建平行机器」。luca 一句「你不是没有做完吗？做完再审查啊」戳破——
**我把同一条宪法的两半用反了**：§4 全文是「优先接 harness 原生原语，不自建平行机器」，
而我的方案正是用 Codex 原生 subagent/`codex exec` 做 fan-out，恰恰是前半句要求的事，
我却拿后半句否决它。真做起来只花一层薄 runner（接 `agent()` 一个原语，编排语义全留在原脚本里，
workflow 零改写），远不是我说的"写一个平行编排引擎"。
连带第二个毛病：**没做完就转去做审查**——审查是收尾动作，前提是东西已经做完。

**Why:** 这是 [[feedback_redteam-own-analysis-before-shipping]] 里 over-claim 的**镜像形态**，
更难自查：over-claim 是夸大做到的（有把柄），这个是**把没做的包装成有意为之**——省略、继承默认、
"保持现状最稳"、"不做硬映射"，每一句单独看都像审慎，合起来是一个空洞。它能骗过我自己的红队，
因为红队问的是"这条结论站得住吗"，而它确实站得住；该问的是"**这条结论回答了那个问题吗**"。
真做的话答案是有的：档位的不变量是**相对序**不是具体模型，Claude 侧投影为模型别名、
Codex 侧投影为 reasoning effort（枚举稳定不随账户失效）——存在一个既不写死模型名、
又真正实现档位的解，我只是没去找。

**How to apply:**
1. **自检句**：交付前对每个"我选择不做 X"问一遍——**这是权衡后的决定，还是我没找到做 X 的方法？**
   两者措辞几乎一样，区别在于我能不能说出**被否决的具体方案及其败因**。说不出 = 回避。
2. **"省略/继承/默认/保持现状"是高危词**。写下它们时必须同时写下：这个默认值在本场景下
   具体会解析成什么，以及它是否真的满足原始需求。答不出就是没设计。
3. **约束不等于结论**。"X 不能写死"是约束，不是方案；从约束直接跳到"那就不做"跳过了
   「在约束内找解」这一步——正是 [[feedback-adoption-ambition]] 母原则说的"别让第二问冒充第一问"。
4. 被问到"这块你怎么处理的"时，**先诚实认领回避再补**，不要为原方案辩护——本次承认
   "我是回避了"之后，正解（映射稳定枚举而非易失效标识符）二十分钟内就找到了。
5. **拿原则否决一件事之前，把那条原则读完整**。自查句：**我引用的这条原则，它的其他部分是不是
   恰好在要求我做这件事？** 框架里的原则多是「优先做 A，不要做 B」的成对形式（Loop 宪法 §4 =
   「优先接 harness 原生原语，不自建平行机器」），只摘后半句当否决理由、而我的方案其实属于前半句，
   是这次的确切错法。成对原则被拆成单句引用时，几乎总是在为一个已经想好的结论找背书。
6. **审查不能替代做完**。没做完就转去做 review，是把"看起来在认真"当成"进展"——
   luca 直接点破「你不是没有做完吗？做完再审查啊」。收尾动作的前提是东西已经做完。

**第 3 次（2026-08-05，两个新变体，都被独立红队当场击穿）：**

- **把"我没找完"写成"这是客观的真冲突"。** Codex 沙箱下 read-only 断网、workflow 需要 gh，
  我在代码注释里写死「这是设计层的真冲突，须人裁决而非默认」。红队实测找到第四选项：
  **codex 沙箱的读是全局的、只有写受工作根约束**，故 `-C <scratch>` 同时拿到网络与仓库写保护——
  我以为必须付出的代价**根本不用付**。"真冲突"是我给"没穷尽搜索"发的合法性证书，
  而且它比单纯不做更隐蔽：**它把一个搜索问题重新定义成了一个需要授权的取舍问题**，
  从而把责任从我转给了 luca。
  自查句：**在写下"这是必须取舍/无解/须裁决"之前，我穷尽搜索了吗？我能说出我试过哪些、
  各自为什么不行吗？** 说不出具体的失败清单 = 我在包装"没找到"。
- **拿现状的缺陷当新决策的授权额度。** 我论证放松沙箱的理由是「Claude Code 侧本来就无沙箱，
  所以这样更严」。红队确认了事实前提（CC 侧确实无沙箱），但击穿推论：
  **"现状已经很松"是一个待修的事实，不是继续放松的额度**。更打脸的是它指出我的 runner 丢弃了
  stdout（工具调用日志走那里），等于把 CC 侧唯一真正在起作用的那层机制——人实时监督——关掉了，
  **实际更松不是更严**。
  自查句：**我用来论证"这样可以"的那个基准，本身是不是一个缺陷？** 拿缺陷当基准 = 缺陷复利。

同族：[[feedback_disclosure-is-not-remediation]]（自陈缺陷≠处理缺陷）、
[[feedback_premise_first_deep_eval]]（摸透系统再给方案，跳步=不负责任）。
