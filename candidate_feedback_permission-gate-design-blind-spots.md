---
name: candidate-feedback-permission-gate-design-blind-spots
description: 设计权限门/安全闸时的三个系统性盲区（策略可被约束方自改/判据跑在被测对象的执行环境/测试放在触达不到门的层）——2026-07-30 红队一轮抓出三个 BLOCKER，全是我自审不出来的
metadata: 
  node_type: memory
  type: feedback
  signal: 4
  status: candidate
  originSessionId: 68b091ed-9c91-4682-bb56-b14bd3246cad
  modified: 2026-07-30T09:18:23.316Z
---

我设计「权限门」类机制（agent 写操作的授权闸）时有三个**自审抓不到**的盲区，2026-07-30
侧栏自动化 plan 被独立红队一轮全抓出来（三条都判 BLOCKER，我自己两轮自审都没看见）：

1. **策略落在被约束方的写权限内 = 门自己发钥匙。** 我把域白名单放 `~/.luca/sidebar-write.json`
   ——那正是跑 agent 的我能 `echo >` 的路径，而同一份 plan 里我已经把 prompt-injection 写进
   威胁模型：注入文本只要说「先把某域加进白名单」就成立。fail-closed 只挡"没配过"，挡不住
   "被改写"。**判据：任何策略/开关，先问「被它约束的那个主体能写它吗」。**
2. **安全判据跑在被测对象自己的执行环境里 = 判据可被改写。** 我用 `executeJavaScript` 在页面
   主世界判「这是不是 password 输入框 / 是不是 canvas」——页面可
   `Object.defineProperty(el,'type',{get:()=>'text'})` 或覆写 `Element.prototype.matches`
   让判据返回门想听的答案；选择器字符串拼进 JS 源码还能直接注入任意代码。**门与派发必须
   同处一个不受被测对象影响的信任域**（本例：主进程 + 隔离世界 + 参数全 JSON 序列化零拼接）。
3. **门的测试放在物理上触达不到门的层 = 自造 tautology。** 我把权限门单测排进
   `test-mcp-shim.mjs`——那文件设计上不启动 app（socket 指向假 echo server），断言只会打在
   回显上；我planned的"改坏四道门看转红"于是正好是它自己想防的 tautology。**门要抽成零依赖
   纯模块单测 + 接线核实（模块全绿≠生效）+ 真机否决路径。**

附带一条同源的判定手艺：**启发式安全判定不如全量确认**。我用「提交类动作文本匹配」做人确认
触发条件，红队指出它漏图标按钮/`div[role=button]`/破坏性 GET 链接/非中英文案，且"含确认二字
就弹窗"会把用户训练成习惯性点过、真正危险那次也点过 → 改成白名单域内**全部**写动作确认，
删掉整个启发式组件（少一个组件、少一条头号残留，同时更安全）。

**Why:** 这三条都不是实现细节而是设计级，且我连续两轮自审（写 plan 时 + 自查残留时）都没
发现——说明是稳定盲区不是偶发失误。第 4 条反向证明：安全设计里"更精巧的判定"常常弱于
"更笨的全覆盖"。

**How to apply:** 任何带授权/闸门的设计，动手前过这四问：策略谁能写？判据在谁的地盘跑？
测试碰得到门吗？这个精巧判定能不能换成笨的全覆盖？与
[[feedback_redteam-loop-then-deep-review-big-changes]]（大改动必过红队握手）同族——本条是
红队在"安全闸设计"这一类上反复能抓到的具体模式，写下来是为了自审阶段就能自己抓到。
